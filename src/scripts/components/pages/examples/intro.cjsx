React = require('react')
GenericContent = require('../../modules/generic-content')

module.exports = React.createClass
    render: ->
        return (
            <div className="intro" >
                <GenericContent title="Build from scratch">
                    TBD. I'll record the build process here.
                </GenericContent>
            </div>
        )
